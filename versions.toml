# version dependend configurations
[location]
files="./files"
[files]
# dictionary in files will only be merged if the version complies
# format "file name" = "pep508 version range"
"xpaths001.json" = ">=3.1.1,<3.2"
"xpaths002.json" = ">=3.1.2,<3.2"